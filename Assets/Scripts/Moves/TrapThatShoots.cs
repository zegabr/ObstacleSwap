using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TrapThatShoots : Trap
{
    // Start is called before the first frame update
    //fazer esse script atirar bouncy letal balls (quicam no chao e no teto indefinidamente até ficar mt distante do mapa) que nao sao afetadsa pela gravidade 
    //sugestao: angulo inicial de tiro = 10 graus defasado em relacao a reta entre o canhao e o player vivo
    //sugestao 2, velocidade inicial alta
    //sugestao 3, botao que ativa esses canhoes devem estar após a metade do mapa
    //canhao invisivel 2 e 3 devem ser ativadas pelo msm botao, de preferencia um botao perto do final do cenario
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
